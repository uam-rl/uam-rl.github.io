name: "Deploy PDF and HTML to GitHub Pages"
on:
  push:
    branches: [ main, master ]
    paths:
      - 'typst-src/**'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: cachix/install-nix-action@v31
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - name: Build main PDF in sandbox
      run: nix build .#pdf -o result-main-pdf
    - name: Build main HTML in sandbox
      run: nix build .#html -o result-main-html
    - name: Build introduccion PDF in sandbox
      run: nix build .#introduccion-pdf -o result-introduccion-pdf
    - name: Build introduccion HTML in sandbox
      run: nix build .#introduccion-html -o result-introduccion-html
    - name: Create build directory and copy outputs
      run: |
        mkdir -p build
        cp result-main-pdf build/main.pdf
        cp result-main-html build/main.html
        cp result-introduccion-pdf build/introduccion.pdf
        cp result-introduccion-html build/introduccion.html
    - name: Copy main HTML to index
      run: cp build/main.html build/index.html
    - name: Setup Pages
      uses: actions/configure-pages@v5
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './build'
        
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
